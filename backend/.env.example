PORT=5000
DATABASE_URL=mysql://root:password@localhost:3306/tgmcafe
PEXELS_API_KEY=
# Secret used to sign JWT auth tokens. REQUIRED in production.
# Generate a strong random value, e.g. `openssl rand -hex 32`.
JWT_SECRET=
